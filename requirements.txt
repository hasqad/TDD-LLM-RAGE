# TDD-LLM Experimental Framework

pytest>=7.0        # test execution (required)
cosmic-ray>=8.3    # mutation testing (required)
radon>=5.1         # code quality metrics (optional, falls back to AST)
